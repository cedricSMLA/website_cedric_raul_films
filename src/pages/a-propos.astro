---
import Base from '@/layouts/Base.astro';
import Section from '@/components/Section.astro';
---

<Base
  title="À propos - Cédric Raúl Films"
  description="Des films de mariage qui provoquent l'émotion. Approche cinématographique, drone FPV, post-production soignée. Paris & worldwide."
>
  <!-- Hero Section avec LOREN -->
  <Section background="primary" padding="xl" class="rounded-xl mb-8 relative overflow-hidden">
    <!-- Gradient background -->
    <div class="absolute inset-0 bg-gradient-to-br from-doré-patiné/15 via-lin to-pierre/10"></div>

    <div class="max-w-5xl mx-auto relative z-10">
      <div class="text-center">
        <!-- LOREN Signature -->
        <div class="scroll-reveal mb-8">
          <h1 class="text-4xl md:text-5xl font-extralight tracking-[0.3em] text-noir-doux mb-3" style="background: linear-gradient(to right, #2c2c2c, #8b7355); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">
            LOREN
          </h1>
          <p class="text-sm md:text-base text-pierre/70 italic font-light tracking-wide">
            Ce que l'amour révèle au silence
          </p>
        </div>

        <div class="w-24 h-1 bg-gradient-to-r from-transparent via-doré-patiné to-transparent mx-auto my-10 animate-line-reveal"></div>

        <h2 class="scroll-reveal scroll-reveal-delay-1 text-3xl md:text-5xl font-bold text-noir-doux mb-8 leading-tight">
          Une vision
          <span class="block text-doré-patiné italic font-light mt-2">cinématographique</span>
        </h2>

        <p class="scroll-reveal scroll-reveal-delay-2 text-lg md:text-xl text-pierre max-w-3xl mx-auto leading-relaxed">
          Entre précision technique et sensibilité artistique,
          je transforme l'émotion brute de votre mariage en récit visuel qui traverse le temps.
        </p>
      </div>
    </div>
  </Section>

  <!-- L'approche -->
  <Section background="dark" padding="xl" class="rounded-xl mb-8">
    <div class="max-w-6xl mx-auto">

      <!-- Titre minimaliste -->
      <div class="text-center mb-20">
        <h2 class="scroll-reveal text-3xl md:text-5xl font-light text-lin mb-4 tracking-wide">
          L'approche
        </h2>
        <div class="w-16 h-px bg-doré-patiné/40 mx-auto"></div>
      </div>

      <!-- Grid simple 2 colonnes -->
      <div class="grid lg:grid-cols-2 gap-20 items-center">

        <!-- Photo gauche -->
        <div class="scroll-reveal">
          <div class="relative aspect-[4/5] w-full">
            <img
              src="/media/photo/IMG_1094.jpg"
              alt="Cédric Raúl, vidéaste mariage"
              class="w-full h-full object-cover rounded-lg shadow-strong"
            />
          </div>
        </div>

        <!-- Contenu droite -->
        <div class="scroll-reveal scroll-reveal-delay-1 space-y-6">
          <p class="text-lg text-lin leading-relaxed">
            Je filme ce qui compte vraiment : les regards, les silences, les moments volés.
          </p>

          <p class="text-base text-lin/70 leading-relaxed">
            Ingénieur de formation, vidéaste par passion. La montagne m'a appris à attendre le bon moment,
            à capter l'instant sans le forcer.
          </p>

          <p class="text-base text-lin/70 leading-relaxed">
            Drone FPV, 4K, montage soigné. Des outils au service d'une vision :
            transformer l'émotion brute en film qui traverse le temps.
          </p>
        </div>

      </div>

    </div>
  </Section>

  <!-- Signature finale -->
  <Section background="primary" padding="lg" class="rounded-xl mb-8">
    <div class="max-w-3xl mx-auto text-center">
      <div class="scroll-reveal">
        <p class="text-xl md:text-2xl text-pierre font-light italic mb-4 leading-relaxed">
          Votre mariage mérite plus qu'un souvenir.<br class="hidden md:block" />
          Il mérite une œuvre.
        </p>
        <div class="w-16 h-0.5 bg-doré-patiné mx-auto my-6"></div>
        <p class="text-doré-patiné text-base md:text-lg font-medium tracking-wider">
          Cédric Raúl
        </p>
        <p class="text-pierre/60 text-sm mt-2">
          Vidéaste mariage cinématographique • Paris & Worldwide
        </p>
      </div>
    </div>
  </Section>

  <!-- CTA finale -->
  <Section background="dark" padding="xl" class="rounded-xl">
    <div class="max-w-3xl mx-auto text-center">
      <h2 class="scroll-reveal text-4xl md:text-5xl font-bold text-lin mb-6 leading-tight">
        Prêt à raconter
        <span class="block text-doré-patiné italic mt-2">votre histoire ?</span>
      </h2>

      <p class="scroll-reveal scroll-reveal-delay-1 text-lg md:text-xl text-lin/80 mb-10 leading-relaxed">
        Discutons ensemble de votre projet et créons un film qui vous ressemble vraiment.
      </p>

      <div class="scroll-reveal scroll-reveal-delay-2">
        <a
          href="/contact#contact"
          class="inline-block bg-doré-patiné text-noir-doux px-12 py-4 rounded-md font-medium hover:bg-lin hover:text-noir-doux transition-all duration-300 hover:scale-105 shadow-strong"
        >
          Contactez-nous
        </a>
      </div>
    </div>
  </Section>
</Base>

<script>
  // Scroll reveal animations
  function initScrollReveal() {
    const observerOptions = {
      threshold: 0.15,
      rootMargin: '0px 0px -100px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('reveal-visible');
        }
      });
    }, observerOptions);

    // Observer tous les éléments avec les classes scroll-reveal
    const elements = document.querySelectorAll('.scroll-reveal, .scroll-reveal-delay-1, .scroll-reveal-delay-2, .scroll-reveal-delay-3, .scroll-reveal-slide-left, .scroll-reveal-slide-right, .scroll-reveal-fade');
    elements.forEach(el => observer.observe(el));
  }

  // Initialiser après le chargement du DOM
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initScrollReveal);
  } else {
    initScrollReveal();
  }
</script>
